import 'package:flutter/material.dart';

//BoxDecoration 

returnContainerStyle(Color color) {
  return BoxDecoration(
    borderRadius: new BorderRadius.all(
      const Radius.circular(8.0),
    ),
  );
}